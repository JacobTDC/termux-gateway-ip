plugins {
  alias(libs.plugins.android.library)
}

android {
  namespace "com.termux.gatewayip.stub"
  buildFeatures {
    buildConfig false
  }
  buildTypes {
    release {
      minifyEnabled false
    }
  }
}

dependencies {
  implementation(libs.androidx.annotation)
}
